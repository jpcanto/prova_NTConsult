export interface IModal {
    show: boolean,
    image: string,
    name: String,
    gender: String,
    created: String,
    films: Number,
    starships: Number
}

export interface IModalStyled {
    image?: string
}